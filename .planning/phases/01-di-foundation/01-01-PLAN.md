---
phase: 01-di-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
  - lib/main.dart
  - lib/services/sync_service.dart
autonomous: true
requirements:
  - DI-01
must_haves:
  truths:
    - "App compiles and runs after adding Riverpod packages — ProviderScope wraps MyApp"
    - "ProviderScope is at root; setupLocator() still runs alongside it (both systems coexist, no crash)"
    - "SyncService.maxRetries inconsistency is resolved — a private const in sync_service.dart is the single source of truth"
    - "build_runner produces zero errors with Drift + Freezed + Riverpod generators in one command"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Riverpod runtime + codegen packages declared"
      contains: "flutter_riverpod"
    - path: "analysis_options.yaml"
      provides: "custom_lint plugin declared"
      contains: "custom_lint"
    - path: "lib/main.dart"
      provides: "ProviderScope wrapping MyApp"
      contains: "ProviderScope"
    - path: "lib/services/sync_service.dart"
      provides: "maxRetries const extracted"
      contains: "const _maxRetries"
  key_links:
    - from: "lib/main.dart"
      to: "ProviderScope"
      via: "ProviderScope(child: MyApp())"
      pattern: "ProviderScope.*child.*MyApp|ProviderScope.*MyApp"
    - from: "lib/services/sync_service.dart"
      to: "_maxRetries constant"
      via: "op.retryCount >= _maxRetries - 1"
      pattern: "const _maxRetries"
---

<objective>
Add Riverpod packages to pubspec.yaml, configure analysis_options.yaml for custom_lint, wrap MyApp in ProviderScope in main.dart, and extract the maxRetries constant in sync_service.dart.

Purpose: This is the mandatory foundation before any @riverpod annotation can be used. The ProviderScope wrapper coexists with the existing setupLocator() call — both DI systems are alive but no service is duplicated yet. The maxRetries extraction (per D-04) must be the first code commit of Phase 1.

Output: Compilable app with ProviderScope at root; Riverpod codegen ready to use; retry threshold inconsistency eliminated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-di-foundation/01-CONTEXT.md

<interfaces>
<!-- Key existing signatures the executor must know. No exploration needed. -->

From lib/main.dart (current structure):
- main() calls: WidgetsFlutterBinding.ensureInitialized(), await setupLocator(), then manual GetIt registrations, then runApp(const MyApp())
- MyApp is a StatelessWidget; its build() returns ScreenUtilInit(...) wrapping MaterialApp(...)
- callbackDispatcher() is annotated @pragma('vm:entry-point') — DO NOT TOUCH this function

From lib/services/sync_service.dart (line 88):
- retryCount check: `if (op.retryCount >= 2)` — the magic number 2 is the target for extraction
- The value 2 = maxRetries - 1 (retry count is zero-indexed: 0, 1, 2 means 3 attempts)
- Replace `2` with `_maxRetries - 1`

From pubspec.yaml (current):
- dependencies: stacked ^3.5.0, stacked_services ^1.6.0
- dev_dependencies: stacked_generator ^2.0.0, build_runner ^2.4.15
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract maxRetries constant in SyncService (per D-04)</name>
  <files>lib/services/sync_service.dart</files>
  <read_first>
    - lib/services/sync_service.dart — read current content; the magic number `2` is on line 88 inside _processOperation
  </read_first>
  <action>
At the top of lib/services/sync_service.dart, before the class declaration, add:

```dart
const _maxRetries = 3;
```

Then in _processOperation, replace the hardcoded `2` in `if (op.retryCount >= 2)` with `_maxRetries - 1`:

```dart
if (op.retryCount >= _maxRetries - 1) {
```

No other changes to this file. The constant is private (underscore prefix) and co-located with the class that owns it, per D-04. The stacked import and ListenableServiceMixin are NOT removed yet — that is Plan 02.
  </action>
  <verify>
    <automated>grep -n "const _maxRetries" /Users/admin/Documents/offline_first_app/lib/services/sync_service.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/services/sync_service.dart contains the line `const _maxRetries = 3;`
    - lib/services/sync_service.dart contains `op.retryCount >= _maxRetries - 1`
    - lib/services/sync_service.dart does NOT contain `op.retryCount >= 2` (the old magic number is gone)
    - No other lines in the file are changed
  </acceptance_criteria>
  <done>Single constant declaration added; hardcoded retry threshold replaced; file otherwise identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: Add Riverpod packages to pubspec.yaml and configure analysis_options.yaml (per D-01, D-02)</name>
  <files>pubspec.yaml, analysis_options.yaml</files>
  <read_first>
    - pubspec.yaml — read current content to know exact existing structure before modifying
    - analysis_options.yaml — read current content; need to add custom_lint plugin entry
  </read_first>
  <action>
In pubspec.yaml, under the `dependencies:` section, add the following entries after `stacked_services`:

```yaml
  # State Management (Riverpod — Phase 1+)
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.3.5
```

In pubspec.yaml, under `dev_dependencies:`, add after `stacked_generator`:

```yaml
  # Riverpod codegen
  riverpod_generator: ^2.4.3
  custom_lint: ^0.6.7
  riverpod_lint: ^2.3.13
```

IMPORTANT: After adding to pubspec.yaml, run `dart pub get` (or `flutter pub get`) and check the output. If any version constraint is incompatible with the current SDK (^3.11.0) or with Drift/Freezed, adjust to the latest compatible patch that pub.dev resolves. The three Riverpod packages (flutter_riverpod, riverpod_annotation, riverpod_generator) must resolve to the same major version (2.x).

In analysis_options.yaml, add the custom_lint plugin declaration. If the file already has an `analyzer:` block, add `plugins: - custom_lint` under it. If no `analyzer:` block exists, add:

```yaml
analyzer:
  plugins:
    - custom_lint
```

Do not remove any existing lint rules. Append only.

After both files are saved, run:
```
dart pub get
dart run build_runner build --delete-conflicting-outputs
```

Confirm build_runner exits 0 with Drift, Freezed, and Riverpod generators all active.
  </action>
  <verify>
    <automated>grep "flutter_riverpod" /Users/admin/Documents/offline_first_app/pubspec.yaml && grep "riverpod_generator" /Users/admin/Documents/offline_first_app/pubspec.yaml && grep "custom_lint" /Users/admin/Documents/offline_first_app/analysis_options.yaml</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `flutter_riverpod:` under dependencies
    - pubspec.yaml contains `riverpod_annotation:` under dependencies
    - pubspec.yaml contains `riverpod_generator:` under dev_dependencies
    - pubspec.yaml contains `custom_lint:` under dev_dependencies
    - pubspec.yaml contains `riverpod_lint:` under dev_dependencies
    - analysis_options.yaml contains `custom_lint` under `analyzer.plugins`
    - `dart pub get` exits 0 (no version conflict errors)
    - `dart run build_runner build --delete-conflicting-outputs` exits 0
  </acceptance_criteria>
  <done>All Riverpod packages resolvable; build_runner runs Drift + Freezed + Riverpod generators without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Wrap MyApp in ProviderScope in main.dart (per DI-01)</name>
  <files>lib/main.dart</files>
  <read_first>
    - lib/main.dart — read current content; ProviderScope wraps runApp's argument; setupLocator() remains
  </read_first>
  <action>
Add the flutter_riverpod import at the top of lib/main.dart:

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
```

In the `main()` function, change the final `runApp` call from:

```dart
runApp(const MyApp());
```

to:

```dart
runApp(const ProviderScope(child: MyApp()));
```

No other changes to main.dart. In particular:
- `setupLocator()` call stays exactly as-is (line 49)
- All GetIt registrations (database, API client, repos, connectivity, sync) stay as-is
- `callbackDispatcher()` function is NOT touched — it must remain Riverpod-free permanently (platform constraint, per CONTEXT.md architecture constraints)
- `StackedService.navigatorKey` and `StackedRouter` in MyApp.build() stay as-is — routing is Phase 3
  </action>
  <verify>
    <automated>grep -n "ProviderScope" /Users/admin/Documents/offline_first_app/lib/main.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/main.dart contains `import 'package:flutter_riverpod/flutter_riverpod.dart';`
    - lib/main.dart contains `ProviderScope(child: MyApp())`
    - lib/main.dart still contains `await setupLocator();`
    - lib/main.dart still contains `locator.registerSingleton` calls (GetIt not removed)
    - callbackDispatcher() function has no Riverpod import or ProviderScope reference
    - `flutter analyze lib/main.dart` exits 0 (or zero errors — warnings about stacked are acceptable)
  </acceptance_criteria>
  <done>ProviderScope at root; both DI systems alive; app compiles and runs without crash.</done>
</task>

</tasks>

<verification>
Run the following commands from the project root to confirm the plan is complete:

```bash
# 1. maxRetries constant present
grep "const _maxRetries = 3" lib/services/sync_service.dart

# 2. Old magic number gone
grep "retryCount >= 2" lib/services/sync_service.dart  # Should return nothing

# 3. Riverpod packages in pubspec
grep -E "flutter_riverpod|riverpod_annotation|riverpod_generator|riverpod_lint|custom_lint" pubspec.yaml

# 4. ProviderScope in main
grep "ProviderScope" lib/main.dart

# 5. custom_lint in analysis_options
grep "custom_lint" analysis_options.yaml

# 6. build_runner clean
dart run build_runner build --delete-conflicting-outputs
```

All commands must exit cleanly (no errors). The app should launch on device/simulator with behavior identical to before.
</verification>

<success_criteria>
- `const _maxRetries = 3` declared in sync_service.dart; no hardcoded `2` remains for the retry threshold check
- `flutter_riverpod: ^2.6.1`, `riverpod_annotation: ^2.3.5` in pubspec dependencies; `riverpod_generator`, `custom_lint`, `riverpod_lint` in dev_dependencies
- `custom_lint` listed under `analyzer.plugins` in analysis_options.yaml
- `runApp(const ProviderScope(child: MyApp()))` in main()
- `dart run build_runner build --delete-conflicting-outputs` exits 0
- App launches without crash; existing behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-di-foundation/01-01-SUMMARY.md` using the summary template.
</output>

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Primary ───────────────────────────────────────────────────────────────
  static const Color primary        = Color(0xFF3C26BF);
  static const Color primaryLighter = Color(0xFFF6F5FA);
  static const Color primaryLight   = Color(0xFFB1A8E5);
  static const Color primaryMild    = Color(0xFF6351CC);
  static const Color primaryText    = Color(0xFF272727);

  // ── Gray scale ────────────────────────────────────────────────────────────
  static const Color grayDark      = Color(0xFF2B2D33);
  static const Color grayMedium    = Color(0xFF808185);
  static const Color grayLight     = Color(0xFFAAABAD);
  static const Color grayVeryLight = Color(0xFFE9E9E9);
  static const Color white         = Color(0xFFFFFFFF);
  static const Color black         = Color(0xFF000000);

  // ── Green scale ───────────────────────────────────────────────────────────
  static const Color greenLight  = Color(0xFFD3FFCC);
  static const Color green       = Color(0xFF4CAF50);
  static const Color greenMedium = Color(0xFF4F9B56);
  static const Color greenDark   = Color(0xFF3B7440);
  static const Color greenDeep   = Color(0xFF16A34A);

  // ── Orange / Amber scale ──────────────────────────────────────────────────
  static const Color orangeLight  = Color(0xFFFFEBCC);
  static const Color orange       = Color(0xFFEC861E);
  static const Color orangeMedium = Color(0xFFB96312);
  static const Color orangeDark   = Color(0xFFFF8C00);

  // ── Red ───────────────────────────────────────────────────────────────────
  static const Color redLight = Color(0xFFFEE2E2);
  static const Color red      = Color(0xFFFF0000);

  // ── Blue ──────────────────────────────────────────────────────────────────
  static const Color blueLight = Color(0xFFEFF6FF);
  static const Color blue      = Color(0xFF155DFC);

  // ── Emerald ───────────────────────────────────────────────────────────────
  static const Color mintLight = Color(0xFFF0FDF4);
  static const Color emerald   = Color(0xFF00A63E);

  // ── Accent palette ────────────────────────────────────────────────────────
  static const Color purpleLight   = Color(0xFFD8D4F2);
  static const Color lavenderLight = Color(0xFFDADBF2);
  static const Color aquaLight     = Color(0xFFD7F2F2);
  static const Color indigoDark    = Color(0xFF22156A);
  static const Color tealDark      = Color(0xFF3B7C7C);
}
